{% extends 'base.html.twig' %}

{% block title %}Admin Page
{% endblock %}

{% block body %}
	<div class="container">
		<h1>Admin Page</h1>

		<table class="table">
			<tr class="col-md">
				<th scope="col">
					<a href="{{ path('app_line_index') }}">Lines</a>
				</th>
				<th scope="col">
					<a href="{{ path('app_station_index') }}">Stations</a>
				</th>
				<th scope="col">
					<a href="{{ path('app_stop_index') }}">Stops</a>
				</th>
				<th scope="col">
					<a href="{{ path('app_train_index') }}">Trains</a>
				</th>

			</tr>
		</table>
	</div>
{% endblock %}
